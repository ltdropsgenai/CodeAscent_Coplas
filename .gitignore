# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# Metro
.metro-health-check*

# debug
npm-debug.*
yarn-debug.*
yarn-error.*

# macOS
.DS_Store

# env
.env*.local

# IDE
.idea/
.vscode/

# Google Play service-account key — referenced by eas.json submit config.
# This is a CREDENTIAL: it can publish releases to your Play Console.
# Never commit it. Regenerate it in Play Console if it is ever exposed.
play-service-account.json
*-service-account.json
google-services.json

# Scratch
fp.mjs

# Abuela review staging. Regenerated clips land here to be watched BEFORE they
# replace anything in assets/abuela/, and _abuela_prev holds the copy that was
# replaced so a bad take can be undone. Both are large and neither ships.
scripts/_abuela_new/
scripts/_abuela_prev/
